# -CSE473s_Project
This project aims to develop a foundational neural network library from scratch, and validate it on the classic XOR problem. The library is used to train autoencoder network for image reconstruction. The trained autoencoder is used then as feature extractor.
